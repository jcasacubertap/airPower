// -*- C++ -*-

#include "auxiliarInputParameters"

//1.- Tunnel Geometrical Parameters 

 //Basic geometry 

 tunnelInletHeight     900.0;          //[mm] (half height)

 tunnelOutletHeight    900.0;          //[mm] (nule,CP)

 flatPlateLength       7800;           //[mm]
 
 z0                    -10;            // first z-plane [mm]
 z1                     10;            // second z-plane [mm]  (quasi-2D thickness)
 
 // Secondary geometrical parameters
  
 seleAR                4;             //[-] (optional, to include a flat plate with LE)

 seleHalfThickness     10;             //[mm] (optional, to include a flat plate with LE)
 
 lamTurbHeight         40;             //[mm] (optional, region to on/off RANS modelling)

//2.- Grid Discretization Parameters

 //Streamwise partitions
 Nx 210;                              //[-]
 
 //Wall-normal partitions 
 Ny 60;                              //[-]

//3.- Flow Parameters (m as dimensional units)

 freeStreamVelocity    24.84840467482210;       //[ms^1]

 freeStreamViscosity   1.456610719354608e-5;    //[m^2s^1]

//4.- Control points (to set initial condition)
//Authomatically generated (using 'system/auxiliarInputParameters') if
//the optimization routine is run ('./multipleRunOptimization'). If a single
//case is to be run ('./singleRun') then copy 'auxiliarInputParameters' to
//'system'

//5.- First and last streamwise positions of data treatment (xpi and xpf) 
//To be changed in 'mainMatlab'  
